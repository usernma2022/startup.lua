local detector = peripheral.find("player_detector") or peripheral.find("playerDetector")

local ok, result = pcall(function()
    return detector.getPlayersInRange(192)
end)

print("One argument:")
print(ok)
print(textutils.serialize(result))

print("----------------")

local ok2, result2 = pcall(function()
    return detector.getPlayersInRange(0, 0, 0, 192)
end)

print("Four arguments:")
print(ok2)
print(textutils.serialize(result2))
