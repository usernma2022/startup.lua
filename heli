local gain = 40

while true do
    local q = ship.getQuaternion()

    local roll = q.v.x

    local output = math.floor(math.abs(roll * gain))

    if output > 15 then
        output = 15
    end

    redstone.setAnalogOutput("right", output)

    sleep(0.05)
end
